'use client';

import React from 'react';
import ShowcasePage from '../components/ShowcasePage';

/**
 * 展示页面组件
 * 封装ShowcasePage组件，便于在app目录中使用
 */
const ShowcasePageContainer: React.FC = () => {
  return <ShowcasePage />;
};

export default ShowcasePageContainer; 