'use client';

import React from 'react';
import HomePage from '../components/HomePage';

/**
 * 主页页面组件
 * 封装HomePage组件，便于在app目录中使用
 */
const HomePageContainer: React.FC = () => {
  return <HomePage />;
};

export default HomePageContainer; 