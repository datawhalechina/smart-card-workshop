import ShowcasePage from '@/components/ShowcasePage';
import React from 'react';

/**
 * Renders the showcase page for exploring creative cards.
 * @returns {React.ReactElement} The showcase page component.
 */
export default function ShowcaseRoutePage(): React.ReactElement {
  return <ShowcasePage />;
} 